{% set name = "atpbar" %}
{% set version = "2.0.10" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: 4b6e87dae70d726fa5ca9696851880dc1135ca6a404f66be77b7ae8cfb588a0b
build:
  noarch: python
  number: 0
  script: {{ PYTHON }} -m pip install . --no-deps -vv

requirements:
  host:
    - python {{ python_min }}
    - pip
    - hatchling
    - hatch-regex-commit
  run:
    - python >={{ python_min }}
  run_constrained:
    - jupyter
    - ipywidgets
    - widgetsnbextension

test:
  requires:
    - python {{ python_min }}
  imports:
    - atpbar

about:
  home: https://github.com/alphatwirl/atpbar
  license: MIT
  license_family: MIT
  license_file: LICENSE
  summary: Progress bars for threading and multiprocessing tasks on terminal and Jupyter Notebook
  description: |
    atpbar can display multiple progress bars simultaneously growing
    to show the progresses of iterations of loops in threading or
    multiprocessing tasks on terminal and Jupyter Notebook.
  dev_url: https://github.com/alphatwirl/atpbar
  doc_url: https://github.com/alphatwirl/atpbar#atpbar

extra:
  recipe-maintainers:
    - TaiSakuma
